{
  "name": "@isildur-testing/core",
  "version": "0.0.1",
  "main": "dist/index.cjs",
  "types": "dist/index.d.ts",
  "module": "dist/index.js",
  "type": "module",
  "files": [
    "dist/**"
  ],
  "scripts": {
    "build": "tsup-node src/index.ts --format cjs,esm --dts",
    "dev": "npm run build -- --watch",
    "test": "jest"
  },
  "dependencies": {
    "@isildur-testing/api": "workspace:*",
    "@isildur-testing/jest": "^0.0.2"
  },
  "devDependencies": {
    "@types/jest": "^29.5.1",
    "@types/node": "^18.16.3",
    "tsconfig": "workspace:*",
    "tsup": "^6.7.0",
    "typescript": "^5.0.4",
    "jest": "^29.5.0",
    "jest-preset": "workspace:*"
  },
  "jest": {
    "preset": "jest-preset"
  }
}
